{{page-title "New"}}
<button type="button" {{on "click" this.showAlert}} >
  afficher une alerte
</button>
<h1>Liste des Utilisateurs</h1>
<ul>
  {{#each @model as |user|}}
    <li>
     <LinkTo @route="users.show" @model={{user}}> Utilisateur {{user.id}} </LinkTo>
    </li>
  {{/each}}
</ul>
